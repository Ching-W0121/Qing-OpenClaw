
## 2026-03-24 Evening distillation

- 今日下午已完成对求职 Agent（智联招聘主链）的系统性收口，结论比中午更明确：**问题核心已从“搜索页拿不到数据”转移到“状态机与正式入口收口”**，并且搜索层的 500 持续问题已通过重复复测证明被修住。
- 已确认的关键根因：智联搜索页并非失败，`window.__INITIAL_STATE__.positionList` 一直能读到 20 条职位；真正的持续问题在于**状态数据拿到后，后处理逻辑把结果筛空/误判失败，导致代码错误掉回旧 fallback 分支，再触发旧链路 500 与熔断**。这是典型的“逻辑回退陷阱”，不是页面本身持续失效。
- 已完成关键修复 1（搜索层收口）：
  - `zhilian_adapter.py` 中 `_is_valid_job()` 已从“重业务过滤”收缩为“轻结构合法性过滤”，避免搜索阶段提前杀掉过多职位。
  - `_do_search_with_timeout()` 已改成 **fail-open**：只要 `__INITIAL_STATE__` 成功拿到，就优先直接返回结构化结果，不再轻易进入旧的 `snapshot/recommend` fallback。
- 已完成关键验证 1（稳定性复测）：同条件连续 3 轮复测，智联搜索页均稳定返回 20 条职位，且不再复现之前的“500 -> 连续重试 -> 熔断 -> 0 条”的持续故障模式。因此可以认定：**搜索阶段 state_jobs 成功后又掉回旧 fallback 导致 500 的持续性问题已修住**。
- 已完成关键修复 2（URL 保真）：智联详情页 URL 规范化已改为**优先保留新式 `/jobdetail/CC...J....htm` 原始路径**，不再错误降级成旧 numeric URL。这个修复解决了“进入详情页时职位信息走丢”的主问题。
- 已完成关键修复 3（详情页执行器主链）：智联投递已切到**详情页唯一执行主链**，不再依赖列表页直接点“立即投递”。正式原则已明确：列表页只负责 discovery / 提取最新 `detail_url`，详情页才负责真实投递和验证。
- 已完成关键修复 4（按钮定位收口）：通过详情页侦查脚本拿到真实 DOM 证据，确认目标页中存在真实按钮：
  - 标签：`BUTTON`
  - 文本：`立即投递`
  - class：`a-button a--bordered a--filled`
  这已经被固化进 `ZhilianAdapter.apply()` 的优先定位选择器中，按钮定位不再停留在猜测层。
- 已完成关键修复 5（状态机逻辑收口）：当前智联详情页返回结构已统一为：
  - `submitted`
  - `already_applied`
  - `needs_user_action`
  - `failed`
  - `unknown`
  且 `ApplicationService._apply_zhilian()` 已按该状态机消费，不再依赖旧的模糊 `verify.result.success` 口径。
- 已确认的实现 bug 并已修复：投递状态机最初一直落到 `unknown`，根因不是规则错，而是**代码读取 `precheck/postcheck/refreshcheck` 时读错了层级**。浏览器 evaluate 返回结构是 `{ok, targetId, url, result:{...}}`，而之前代码错误地直接读 `precheck.get('already')` 等顶层字段，导致所有状态判断落空。现已统一改为读取 `precheck['result'] / postcheck['result'] / refreshcheck['result']`。
- 已完成关键验证 2（样本覆盖）：连续抓取 5 个最新详情页样本后，真实页面状态链路已经稳定呈现：
  - 点击前：`hasApplyButton=true`，按钮文本为 `立即投递`，且 `pre_already=false`
  - 点击后：`post_already=true`，且 `post_has_apply_button=false`
  - 刷新后：仍为 `已投递`
  因此最终业务规则已经被真实样本验证成立：
  **“点击前是立即投递，点击后/刷新后变成已投递” => 正式判定为 `submitted`，`verified_by=button_state_change` 或 `page_refresh_button_state`。**
- 已完成关键修复 6（第一阶段/第二阶段代码口径收口）：
  - `multi_platform_pipeline.py`、`run_auto_apply_round.py`、`run_vectorbrain_stage.py` 等正式入口已开始切到 `detail_url` 优先口径，不再默认只用旧 `url`。
  - `run_auto_apply_round.py` 已接入第二阶段第一刀：搜索预算控制、阶段时限、全局时限、fallback apply top N，并且汇报口径已开始区分 `submitted / already_applied / failed / dry_run`。
- 当前阶段性结论：**智联搜索层 + 详情页投递层 + 最后一层状态机，已经基本完成新主链收口。** 现在的主要工作从“修这条主链是否可用”转移为“清理正式入口的旧口径残留 + 清理调试/临时脚本残留，确保以后不会再误触老路”。
- 仍然未收口/待后续处理的事项：
  1. OpenClaw 原生子会话编排仍未真正跑通，当前主链仍主要由主进程兜底执行；
  2. 正式仓库中仍存在若干历史 `tmp_*.py / debug_*.py / probe_*.py` 调试脚本，以及少量 `url` 优先的老引用，需要做一次“旧链路残留清理”；
  3. 其它平台（前程无忧、猎聘等）尚未像智联这样完全切到“详情页唯一执行主链 + 状态机收口”，但当前主矛盾已不在智联搜索层。
