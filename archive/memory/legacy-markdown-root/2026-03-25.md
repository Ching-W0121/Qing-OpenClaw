
## 2026-03-25 子会话链路修复与智联并行拆分记录

- 今天定位并修复了 OpenClaw `sessions_spawn(runtime=subagent)` 无法使用的问题。
- 根因是本地 OpenClaw 安装里的 `sessions_spawn` 实现对 `streamTo` 缺少容错：当前调用链会自动附带 `streamTo:"parent"`，而后端对 `runtime=subagent` 直接报错。
- 已在本机 `C:/Users/TR/AppData/Roaming/npm/node_modules/openclaw/dist/pi-embedded-CbCYZxIb.js` 打补丁：当 `runtime!="acp"` 时忽略 `streamTo`，而不是直接拒绝请求。
- 修复后已成功拉起并执行一个真实 subagent 测试，确认子会话恢复可用；ACP 仍不可用，原因是未配置 `acpx runtime plugin`。
- 随后按“每个子会话只做一件事”的方式拆分了 5 个智联任务：
  1. 原始搜索 1-3 页
  2. 原始搜索 4-6 页
  3. 原始搜索 7-10 页
  4. 候选池处理链脚本生成
  5. 投递执行器脚本生成
- 三段原始搜索结果文件都已落盘：
  - `qing-agent/data/zhilian_raw_pages_1_3_20260325_114555.json`
  - `qing-agent/data/zhilian_raw_pages_4_6_20260325.json`
  - `qing-agent/data/zhilian_raw_pages_7_10_20260325_114612.json`
- 候选池处理脚本与投递执行器也已落盘：
  - `qing-agent/data/zhilian_candidate_pipeline_20260325.py`
  - `qing-agent/scripts/zhilian_apply_executor_20260325.py`
  - `qing-agent/data/zhilian_apply_executor_20260325.md`
- 但主流程继续推进时暴露出新的关键问题：原始搜索结果文件内容结构不对，装进去的基本是搜索页 URL/占位记录，而不是“真实职位列表”。
- 因此候选池脚本结果为：
  - 去重后 15
  - 候选池 0
  - 拒绝 15（主要原因 `role_family_not_allowed`）
- 真实阻塞点已确认：不是投递速度问题，而是“智联原始抓取子会话输出结构错误”，需要先修原始抓取，拿到真实职位数组，才能重新跑候选池和投递。

## 2026-03-25 用户明确偏好：追求“最好的效果”

- 用户明确说：我需要的效果是最好的效果，你记住。
- 这条偏好在后续执行中应理解为：
  - 优先追求最终效果与真实完成度，而不是过程好看。
  - 对强时效任务（如招聘搜索/筛选/投递）要优先选择最能保证结果的执行方式。
  - 需要为了最佳结果动态调整实现路径，但不要牺牲最终完成质量。

## 2026-03-25 运行原则正式收口

- 已把“同一种方法、同一套代码路径优先复用；一旦信号不对立即停下反思后再重试”正式写入运行规则。
- 当前正式原则：
  1. 所有复杂任务先过记忆系统与反思系统。
  2. 一旦某类任务已经找到 last-known-good path，默认必须复用。
  3. 如果结果异常、信号变化、页面状态不对，必须立刻停下，先校验/反思，再继续。
  4. 禁止无脑重复执行，禁止临场一天一换路径。
- 已继续禁用多条 legacy memory/import/migration 路径，并把最近多天的 daily markdown 与 learnings 补进 SQLite + 向量层。
