## 2026-03-17 Memory Flush

- Unified the memory architecture around `memory/database/optimized_memory.db` as the single source of truth.
- Disabled incompatible built-in OpenClaw `memorySearch` because `doubao-embedding-vision-251215` does not support the standard OpenAI embeddings API; semantic retrieval now runs through our own `memory/vector_memory.py` against `/embeddings/multimodal`.
- Kept main chat model on `custom-redeem-ai-online/gpt-5.4`; 豆包 is used only for embedding / semantic retrieval.
- Reworked memory pipeline to: writer=`memory/unified_memory.py`, retriever=`memory/memory_search.py`, compatibility wrapper=`scripts/memory_search.py`, vector layer=`memory/vector_memory.py`.
- Migrated historical memory into SQLite main DB; verified date coverage currently spans `2026-03-11` through `2026-03-17`.
- Cleaned a corrupted/garbled test memory record from the main DB, vector table, and JSONL backup.
- Completed vector sync explanation: main DB has 551 memory items; vector layer reached 484 items after adding summaries/profiles, with the remaining gap mostly caused by empty episode content and deduplicated repeated short messages.
- Confirmed current active system is our own unified memory stack, not the old VectorBrain integration.
- Removed active VectorBrain integration from live paths by moving these into backup `C:\Users\TR\.openclaw\cleanup-backups\vectorbrain-removal-20260317-104251`:
  - `C:\Users\TR\AppData\Roaming\openclaw\skill.json`
  - `C:\Users\TR\AppData\Roaming\openclaw\connector\`
  - `C:\Users\TR\AppData\Roaming\openclaw\config\`
  - `C:\Users\TR\.openclaw\vectorbrain-main\`
- Also cleared workspace runtime residue tied to VectorBrain: `memory/vectorbrain.log` and related `memory/__pycache__` artifacts.
- Historical mentions of VectorBrain remain in episodic memory / lessons / browser history as history only, not active integration.

## 2026-03-17 下午补记 - 求职 Agent 运行结果

- 下午轮（plan: `afternoon`）实际执行，报告文件：`qing-agent/data/auto_apply_reports/auto_apply_afternoon_20260317_160552.json`
- 执行时间：开始 `2026-03-17T16:03:31`，结束 `2026-03-17T16:05:52`
- 本轮配置仍为深圳、排除宝安、阈值 `0.75`、允许桶 `apply`
- 平台结果：
  - 智联招聘：`品牌策划` 找到 2 个、`品牌设计` 找到 0 个
  - 前程无忧：0 个
  - 猎聘：0 个
- 智联本轮筛出的 2 个可投递岗位：
  1. 活动策划经理｜深圳-福田｜1-1.5万｜动态评分 84.8%｜结论：可投递
  2. 活动策划执行｜深圳-龙岗｜1-1.5万｜动态评分 84.8%｜结论：可投递
- 但本轮 `apply_results` 状态为 `dry_run`，说明已完成抓取/评分/判定，但没有进入真实投递提交。
- 上午轮（report: `qing-agent/data/auto_apply_reports/auto_apply_morning_20260317_145619.json`）结果基本为空：智联 0、前程无忧 0、猎聘 0。
- 下午同时更新了若干求职 Agent 相关文件：`qing-agent/qing_platform/zhilian_adapter.py`、`qing-agent/tools/dynamic_scorer.py`、`qing-agent/tools/industry_semantic_matcher.py`，以及 `qing-agent/data/job_vault.db` / `job_vault.index` / `qing_agent.db`。
- 重要异常：本次下午运行没有被实时同步到主记忆链路中；`memory/working/session_current.json` 仍停留在 2026-03-11 的旧目标，`memory/auto-20260317.json` 也没有记录本次求职 Agent 运行结果。

## 2026-03-17 夜间补记 - 求职 Agent 策略校准与链路排查

- 已真实验证智联招聘自动投递闭环：`ZhilianAdapter.apply()` 能点击“立即投递”，页面校验 `verify.result.success=true`，并已通过 `ApplicationService` 成功写入 `applications` 表（新增 `application_id=7`，`status=applied`）与 `matches.is_applied=1`。
- 已将 `ApplicationService` 的智联投递主链路切换为统一走下午修好的 `ZhilianAdapter.apply()`，不再使用旧的 `_apply_zhilian()` 点击逻辑作为主路径。
- 已确认记忆主库与向量层此前不同步：`episodes` 能写入，但 `memory_vectors` 不会自动更新；已在 `run_auto_apply_round.py` 中补入 `upsert_vector(...)`，使求职 Agent 轮次报告在写入主记忆后同步进入向量层。
- 对“品牌策划 / 品牌设计”做了多轮 dry-run 排查。结论：智联对“品牌策划”及其近义词（如品牌传播、品牌推广、品牌企划等）的搜索结果存在明显设计岗污染，很多搜索词会回大量平面/视觉/包装设计岗位。
- 已做品牌策划词组召回诊断，发现相对有效的词主要是：`品牌策划`、`文案策划`、`品牌推广`、`品牌企划`、`市场策划`、`品牌经理`；明显污染严重的词包括：`品牌传播`、`活动策划`、`品牌运营`、`新媒体经理` 等。
- 用户已明确更新投递偏好：
  - 优先偏向策划岗位；
  - 后续进一步收紧为“必须是策划岗，排除运营类岗位”；
  - 行业属性必须达标；
  - 不要求一定带“品牌”二字；
  - 之后又允许“行业语义相似度 >= 0.70 即视为行业匹配”。
- 基于上述偏好，已将策略理解收口为：`策划岗 only` + `排除运营` + `行业语义相似度 >= 0.70` + `品牌标签不是必须`。
- 在该严格规则下，智联第一页附近可筛出的岗位很少；经过扩深到智联第 1~3 页后，稳定拿到 3 个候选：
  1. 美居活动策划岗（南山，活动）；
  2. 营销策划总监（南山，语义命中科技智造与 AI+/文旅）；
  3. 摄影俱乐部活动策划（福田，活动）。
- 这表明：在当前严格规则下，智联确实能出符合条件的策划岗，但总量偏少；翻页扩深有效，但提升幅度有限（从约 2 个提升到 3 个）。
- 已确认前程无忧（51job）页面本身正常：搜索页可打开、已登录、页面正文存在真实职位，如“品牌策划”“品牌经理”“品牌策划专员”“市场企划助理”等；因此“0 结果”并非平台无岗位，而是适配器未正确解析页面。
- 进一步排查前程无忧：
  - 原 `browser.evaluate(...)` 大段 DOM 抽取逻辑会触发 `HTTP Error 500`；
  - 改为 `aria snapshot` 路径后仍未成功返回结构化岗位，说明当前主问题已收敛到“snapshot 节点解析规则未对准真实职位卡片结构”。
- 已确认前程无忧当前状态：页面可用、真实岗位存在，但 `Job51Adapter.search_jobs()` 仍未真正修通；后续需要专门针对 51job 的 aria 节点树做定点解析，而不是继续把 0 结果当作真实供给。
- 已为智联增加真实分页能力：`page > 1` 时直接打开 `https://www.zhaopin.com/sou/jl765/p{page}?srccode=401801`，并已成功验证第 2 页可返回真实职位列表，不再是仅打印页码的假分页。
