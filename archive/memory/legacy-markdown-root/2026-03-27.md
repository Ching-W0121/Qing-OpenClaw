## 2026-03-27 durable memory flush

- 今天已把“监控存在但不自动入库”的断点补齐：`memory/watchdog.py` 现在会检查前一天 `memory/YYYY-MM-DD.md` 中的 `durable memory flush`，若主库 `episodes` 缺少对应下午/晚间记录，则自动执行 backfill 写入 SQLite 主库、episodic JSONL，并记录 `watchdog_last_backfill.json`。
- 已用新 watchdog 闭环成功补回 2026-03-26 下午的缺失主记忆：补写条目时间戳为 `2026-03-26T23:59:00+08:00`，标题为 `2026-03-26 durable memory flush（watchdog backfill）`。这证明“监控 → 自动补账”闭环已经真实生效。
- 今天已新增统一业务记忆桥接器：`qing-agent/scripts/memory_bridge.py`，对外提供 `record_job_task_completion(...)`，统一完成 episodes 主库写入、episodic JSONL、working/session_current.json、working event 与向量写入，避免各业务脚本各写各的记忆逻辑。
- 目前已接入统一即时入库的正式求职 Agent 主链入口包括：
  - `qing-agent/scripts/run_auto_apply_round.py`（原本已接）
  - `qing-agent/scripts/run_vectorbrain_stage.py`
  - `qing-agent/scripts/run_vectorbrain_orchestrator.py`
  - `qing-agent/scripts/run_dual_platform_apply_strict.py`
  - `qing-agent/scripts/run_dual_platform_brand_scan.py`
  - `qing-agent/scripts/memory_bridge.py` 作为统一桥接层
- 今天对求职 Agent 做了硬收口清理，不只是标记废弃，而是物理删除旧链路。第一轮已删除 53 项，包括：
  - 旧批次/一次性脚本：`run_dual_platform_brand_apply_full.py`、`run_zhilian_brand_apply_full.py`、`run_zhilian_apply_batch_B_20260325.py`、`zhilian_apply_batch_A_20260325.py`、`zhilian_apply_batch_C_20260325.py`、`zhilian_apply_executor_20260325.py`、`run_zhilian_raw_pages_7_10_20260325.py`、`_zhilian_probe.py`、`_inspect_zhilian_nodes.py` 等
  - 旧候选管线/after_patch/full_fixed/raw_pages/reason_breakdown 产物
  - `qing-agent/data/brand_scan/` 分片目录
  - `qing-agent/vectorbrain/message_queue.jsonl`、`message_summary.json`、`stagger_run_log.jsonl`
  - `qing-agent/` 下全部 `__pycache__`
- 第二轮深清又删除 5 项：
  - `debug-archive/` 整个目录
  - workspace 根 `.pytest_cache`
  - `memory/__pycache__`
  - `scripts/__pycache__`
  - `qing-agent/data/auto_apply_reports/zhilian_apply_executor_20260325_20260325_133516.json`（其内容直接引用已删除的旧 candidate pipeline 输入）
- 经过两轮清理，当前保留并视为正式主链的求职 Agent 入口明确固定为：
  - `run_auto_apply_round.py`
  - `run_vectorbrain_stage.py`
  - `run_vectorbrain_orchestrator.py`
  - `run_dual_platform_apply_strict.py`
  - `run_dual_platform_brand_scan.py`
  - `memory_bridge.py`
- 今天再次确认：昨天求职 Agent 真正停下来的位置不是“抓取链坏”，而是“候选分析链可信但 `apply_count = 0`”。昨天最终可信状态为：智联 1–10 页候选池已重跑完成，标准化岗位 400，去重后 241，`candidate_count = 6`、`reject_count = 235`、`apply_count = 0`、`backup_count = 6`。当前主问题已转为规则/边界/阈值，而不是抓取或展平错误。
- 今天明确给用户的最顺推进建议是：不要回头重修搜索链路，而是直接解剖昨天那 6 个 `backup` 候选，逐条确认它们没进 `apply` 的精确原因（hard gate / role family / dynamic scorer / threshold），再只改一条最小规则做小范围复跑。
- 今天用户要求把当前所有状态和问题重新提交给 Gemini 咨询方案；但按已加固的执行闸门规则，任务 `通过浏览器 Gemini 提交求职 Agent 当前状态、问题与求解请求` 被 `preflight_guard.py` 判定为 `BLOCKED`，原因是：
  - `slow_task_requires_subsession`
  - `external_channel_requires_probe_first`
  - `probe_not_confirmed`
  也就是当前浏览器外部咨询任务必须先满足 probe/channel 确认，或得到用户明确放行，不能在现有硬闸门下直接偷跑。
- 今天一个重要元结论被再次坐实：真正的“收口”必须同时覆盖三层——
  1. 业务主链只保留正式入口
  2. 旧代码/旧数据/旧缓存物理删除
  3. 结果写入主记忆必须原生接入主链，并由 watchdog 兜底补账
  只做其中一层都不算真正收口。
