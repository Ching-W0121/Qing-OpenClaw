
## 2026-03-23 持久记忆补充（会话压缩前）

- 用户明确纠正：求职工作流默认应为 **搜索 → 匹配 → 去重 → 返回表格**，中间步骤不需要过程性汇报，**除非出现错误**。
- 用户明确纠正：主进程在汇总子进程结果时，**去重必须先完成**，不能把未去重候选直接汇报给用户。
- 用户明确要求：此类多岗位搜索/筛选/投递任务，**优先使用 OpenClaw 子进程/子会话编排**，而不是 Python 进程做主流程；主进程负责调度、硬过滤、去重、汇报，子进程只执行与回传结构化数据。
- 用户明确提醒：**浏览器/Gemini 页面异常时，优先清理浏览器缓存再重开**，这是高频排障步骤，应当优先记住并执行。
- 今日已确认的重要技术结论：智联搜索主链更稳定的事实源不是脆弱 DOM，而是页面内嵌的 `window.__INITIAL_STATE__.positionList`；这是后续智联适配器的主读取入口。
- 今日用户确认的岗位判断：
  - 广东启悦未来科技有限公司：放入可投池；
  - 深德科两条设计岗：视为近似重复岗；
  - 湖南太阳龙医疗科技有限公司：整体偏平面设计，不归为品牌类型，排除；
  - 深圳市科陆电子科技股份有限公司：年限/经历不符合，pass。
- 今日真实业务判断：智联某条岗位实际已投递成功；后续智联成功判断不能只靠 `alreadyApplied` 文案，还应优先检查页面是否出现 **“恭喜您，投递成功！”**，必要时再去“求职反馈”核验。
