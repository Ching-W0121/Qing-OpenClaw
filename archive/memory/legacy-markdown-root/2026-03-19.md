## 2026-03-19 持久记忆补充 - 记忆系统收口 / 求职 Agent 失败诊断 / VectorBrain 编排层落地

### 一、记忆系统在今天上午完成的关键收口
- 会话层默认读取入口已切到 **SQLite 主库 / `memory_search.py` 优先**，不再 `md-first`。
- `memory/*.md` 与 `MEMORY.md` 已重新定义为 **摘要/兼容视图**，不再作为默认事实入口。
- `memory/reflection-worker.js` 已恢复为活动 workspace 下可运行的正式 worker，并已验证跑通。
- 记忆统计口径已修正：
  - `episodes` = 结构化事件数
  - `knowledge` = 结构化知识条目数
  - `summaries` = 摘要条目数
  - `profile` = 用户画像字段数
  - `vectors` = 向量索引规模
  以后不再把 `episodes` 混报成“总记忆数量”。
- 排查并清理了旧碎片向量来源：`scattered_file.*`、`memory_file.*`、`recent_memory_file.*`、`reference_file.*` 等历史导入碎片从主召回空间移除。
- 清理结果：历史碎片 knowledge vectors 删除 1069 条，主向量空间从约 1554 压到 485 左右，主召回层显著干净。
- 诊断结论：当前语义查询的主要延迟瓶颈更可能在 **Python 启动 / embedding & 语义执行链 / rerank**，而不再是向量规模本身。

### 二、今天中午前的求职 Agent 失败原因（需要长期记住）
- 今天重跑智联轮次，虽然压住了上一轮“同职位重复投递”的 bug，但新的失败主因不是投递按钮，而是：
  - **搜索策略过重**
  - 关键词家族铺得太散
  - 大量时间耗在“搜索 → 抓详情 → 评分”阶段
  - 没有在合理时间内完成真实投递主目标
- 用户明确判定：
  - “这一轮算失败的”
- 这条必须长期记住：
  - **搜索成功 ≠ 投递成功**
  - 对求职 Agent 来说，真正的成功标准是：
    - 在可接受时间内完成真实投递
    - 而不是只完成搜索、抓取、评分。

### 三、今天修掉的重复投递 bug（已落代码）
- 在 `qing-agent/scripts/run_auto_apply_round.py` 增加了整轮级别去重：
  - `round_seen_apply_keys`
  - 同 URL / 同平台键只允许进入一次 `batch_apply`
- 在 `ApplicationService.batch_apply()` 增加投递前 guard：
  - 若同 `job_id` 或同平台 URL 已存在 `applied` 主记录，则直接 `skipped_duplicate`
- 在 `ZhilianAdapter.apply()` 中补上：
  - 页面出现 `已投递 / 已申请 / 已申请职位 / 重复投递` 时，返回 `already_applied`
  - 不再误判为 `success`
- 这说明：
  - “同职位跨关键词重复进入投递队列” 这个 bug 已被主链压住。

### 四、用户在 2026-03-19 13:44 后给出的新体系要求（非常关键）
用户明确要求把新的业务编排体系收进 **求职 Agent 系统**，而不是并入记忆系统。边界已经厘清：
- **求职 Agent 负责**：
  - 岗位搜索
  - 职位详情抓取
  - 评分筛选
  - 去重
  - 投递执行
  - 子进程调度
  - 统一消息出口
- **记忆系统负责**：
  - 主动记忆
  - 双端共享记忆
  - 任务结果落库
  - 反思
  - 蒸馏
  - 检索

### 五、用户给求职 Agent 的新硬规则（已开始落地到 qing-agent/vectorbrain）
1. **策划岗位收口**到以下列表：
   - 品牌策划
   - 品牌专员
   - 品牌主管
   - 策略策划
   - 策略专员
   - 活动策划
   - 广告策划
   - 营销策划
   - 产品策划
2. **平台/岗位/步骤可以拆成子进程**，但必须由总编排器统一调度。
3. **子进程不能私自发送消息**，所有消息必须统一走一个总脚本/总线集中管理。
4. **所有代码运行逻辑必须统一**，不能今天 1 套明天 2 套；只有平台差异或动态元素适配时允许不同 adapter 逻辑。
5. 所有这条业务编排相关新文件，统一收进：
   - `qing-agent/vectorbrain/`

### 六、今天已经落进 qing-agent 的 VectorBrain 编排层（重要进展）
已新增：
- `qing-agent/vectorbrain/job_taxonomy.py`
- `qing-agent/vectorbrain/execution_policy.py`
- `qing-agent/vectorbrain/message_bus.py`
- `qing-agent/vectorbrain/message_hub.py`
- `qing-agent/vectorbrain/orchestrator.py`
- `qing-agent/vectorbrain/stagger_runner.py`
- `qing-agent/vectorbrain/system_contract.json`
- `qing-agent/vectorbrain/runtime_config.json`
- `qing-agent/scripts/run_vectorbrain_stage.py`
- `qing-agent/scripts/run_vectorbrain_orchestrator.py`
- `qing-agent/scripts/export_vectorbrain_summary.py`

### 七、VectorBrain 当前已落地的能力
- 岗位词标准与岗位白名单已进入正式代码。
- fast-lane 主关键词策略已定义：
  - 品牌策划：`品牌策划`、`品牌主管`
  - 品牌设计：`品牌设计`、`视觉设计`
- `run_auto_apply_round.py` 已接入：
  - `vectorbrain.job_taxonomy`
  - `vectorbrain.execution_policy`
  - `vectorbrain.message_bus`
  - `vectorbrain.orchestrator`
- 当前主链已经开始限制：
  - 每个 target 最多只跑 2 个主关键词
  - 子流程消息写入消息总线
  - 生成错峰阶段计划
  - 标题白名单过滤，避免岗位无限扩散
- 当前体系状态：
  - 已从“骨架层”推进到“**可执行编排层**”
  - 但 **智联 / 前程无忧完整真实投递轮次** 还没有完全切到 VectorBrain orchestrator 主链上

### 八、需要未来继续推进的重点（高优先级）
- 把智联 / 前程无忧完整真实投递主链彻底切到 `run_vectorbrain_orchestrator.py` 调度下。
- 不再使用旧的“关键词家族全量轮询”作为主逻辑。
- 让统一消息出口真正接到最终汇报器，而不是只停在总线和汇总文件层。
- 以后求职 Agent 的成功标准统一为：
  - **在合理时间内完成真实投递**
  - 而不是仅仅完成搜索、抓取和评分。

## 2026-03-19 晚间补充记忆 - GPT 浏览器咨询链路首次真实跑通

- 今晚围绕“电脑端继续问 GPT / Gemini 的问题”持续排查浏览器咨询链路，用户多次强调：**必须先按 2026-03-19 当天下午的时间点查记忆，再继续问题；不能跨天混合、不能一通输出所有旧记忆。**
- 用户明确纠正并确认：下午 16:20 那段关于“策划岗位边界 / 设计岗位边界 / 为什么想做策划 / 为什么找品牌设计”的长文本，就是要原样拿去问 GPT 的核心原话。
- 本轮浏览器链路按三次原则完成了第一次真实打通：
  1. 打开 `https://chatgpt.com/`
  2. 旧 aria ref 填写失败后，不再死磕旧 ref，改用页面内 evaluate + 定位真实输入框
  3. 成功把 16:20 那段原话真实写入 ChatGPT 输入框，并通过 snapshot 验证 `textbox.value` 与页面可见文本都存在
  4. 旧发送按钮 ref 漂移后，改用页面脚本直接定位“发送提示”按钮并点击
  5. 成功进入真实会话页：URL 变为 `https://chatgpt.com/c/69bbd3ec-e0a4-83e8-9d67-a0d409328fe2`
  6. 会话标题生成：`岗位边界与发展路径`
  7. 页面出现 `你说：` 与 `ChatGPT 说：`，确认 prompt 回显与回复流都成立
- GPT 第一轮回复的核心结论：用户的真实定位不是“设计 or 策划二选一”，而是**“设计向策划过渡型人才（中间态）”**；建议优先走“带策略属性的设计岗 / 初级策划参与岗”，路径是**设计 → 策略型设计 → 半策划 → 策划**。
- GPT 第二轮追问已成功继续发送，内容聚焦两点：
  1. 结合**智联招聘 / 前程无忧 / 市面真实招聘样本**调参，识别“看起来像品牌策划/品牌设计但实际应排除”的混淆岗位，并提出贴近真实平台的筛选规则
  2. 输出投递策略分层：**保底 / 冲刺 / 赌高位**，分别针对品牌策划和品牌设计两条线给出建议
- 用户额外要求：看到实时截图证明是否真正写入 / 是否真正回复；已对当前 GPT 会话页面截图并发回飞书当前会话，供用户目视核验。
- 这次链路的重要教训被再次坐实：
  - 不能再把“程序层写入”误当成真实输入成功
  - 只有同时满足**输入框可见文本存在 → 发送动作真实触发 → 页面进入回复流 / 新会话页**，才允许宣称“已经问到 GPT”
  - 对动态网页（尤其 ChatGPT/Gemini）要采用：**固定链路，动态定位元素**，而不是今天 A 明天 B 随机切方法
