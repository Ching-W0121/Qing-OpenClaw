
## 2026-03-18 补充记忆 - 下午平台口径 / 入库修复 / 求职进展核对

- 下午继续围绕“真实投递结果与数据库/平台口径不一致”做排查与修复。用户明确指出：真正可信的投递口径应以平台前台页面为准，而不是先信本地 report 或 DB。
- 先后核对了平台真实页面：
  - 前程无忧 `我的申请` 页面显示 **社会申请 18**。
  - 智联 `我的投递` 页面显示 **31 我的投递**。
  - 因此统一口径更新为：总投递 = 智联 31 + 前程无忧 18 = 49；今日累计投递按平台前台进展页当天日期统计。
- 通过浏览器直接读取页面文本确认：
  - 前程无忧今日（2026-03-18）可见 4 条真实申请：
    1. 品牌设计师｜深圳立墨文化传播有限公司
    2. 品牌设计师｜优之科技（深圳）有限公司
    3. 品牌策划专员｜深圳市欣博跃电子有限公司
    4. 品牌营销负责人/双休/办公地罗湖｜深圳市武测空间信息有限公司
  - 智联 `求职进展` 页确认：
    - 被查看：3 条（品牌经理 / 新媒体经理/品牌经理 / 活动策划执行）
    - 有意向：1 条（品牌平面设计师｜天津小谷顿餐饮管理服务有限公司）
    - 不合适：10 条（高级平面设计师、视觉设计师、品牌设计/平面视觉设计、品牌设计师双休14薪、品牌设计师、营销视觉设计师-社媒方向、品牌设计师、视觉设计师、营销策划经理、摄影俱乐部活动策划）
- 用户要求两件事：
  1. 今日累计投递和总投递都必须统一口径（以平台真实页面为准）
  2. 继续深挖求职进展，并分析“不合适”公司名单，标注不合适理由
- 已根据页面名单输出过一版“不合适公司名单 + 可能原因”，但明确说明：页面本身未展示 HR 的明确拒绝文本理由，当前理由属于基于岗位方向/级别/行业的推测分析。

### 数据库与投递记录入库链路修复（下午）
- 更正了一个关键误判：真正业务库是 `qing-agent/data/qing_agent.db`，不是旧的 `qing-agent/qing_jobs.db`。`qing_agent.db` 内存在 `applications / jobs / matches / users / alembic_version` 表。
- 对今日真实双平台投递 report（`qing-agent/data/auto_apply_reports/auto_apply_real_dual_platform_20260318_105135.json`）与 `applications` 表做了对账：
  - report 33 条结果中，原已匹配 16 条
  - 缺失 17 条，已通过对账脚本补写入库
  - 一度使 `applications_total` 从 28 增至 45
- 随后发现：补写脚本采用逐条插入，造成同一 `user_id + job_id` 出现多条 `already_applied / submitted` 记录，不符合正式要求。
- 为消除重复，执行了“重复 applied 记录压平”：
  - 发现重复组 5 组
  - 删除重复行 17 条
  - 更新保留主记录 5 条
  - 压平后 `applications_total` 回落至 **28**
  - 关键主记录示例：
    - 51job job_id 826 / 832 最终 `platform_status = submitted`
    - 51job job_id 813 / 814 / 816 最终 `platform_status = already_applied`
- 随后把智联今日这批 `status=applied 但 platform_status is null` 的记录统一补齐为 `platform_status = submitted`：
  - 共更新 16 条
  - 涉及 2026-03-18 10:49~11:00 期间的今日智联真实投递记录
- 用户明确指出：要的是“每次真实投递都能精准倒车入库”的**正式能力**，不是事后临时补丁。已据此将临时修复思路上升为正式代码修复要求：
  - 实时入库
  - 幂等 upsert（`user_id + job_id`）
  - 统一 `platform_status`
  - report 与 DB 结果一致

### 正式代码主链修复（已落到代码）
- `qing-agent/repositories/application_repo.py`：
  - `get_application(user_id, job_id)` 改为优先返回**最新**记录，不再依赖旧的 `first()` 顺序
  - 新增 `get_applied_record(user_id, job_id)`，专门获取 `status='applied'` 主记录
- `qing-agent/services/application_service.py`：
  - `create_or_update_applied_record()` 改为优先走 `get_applied_record()`，实现同一 `user_id + job_id` 的正式 upsert：存在则更新，不存在才插入
  - 更新时增加状态合并逻辑：若新结果是 `submitted`，则保留/升级为 `submitted`，避免被 `already_applied` 覆盖掉真实成功
  - `persist_successful_application()` 增加默认 `platform_status` 归一逻辑：
    - 智联 `status=success` 默认补为 `submitted`
    - 51job `status=success` 默认补为 `result_kind or submitted`
- 已做一次最小回归验证：重复对已存在的 51job job_id=832 走 `persist_successful_application()`，返回的仍是同一条应用记录（`app_id=27`），且 `applications_total` 保持不变，证明正式主链已具备幂等 upsert 能力。

### 智联平台真实状态同步回数据库（下午后段）
- 基于平台真实页面名单，执行了一轮智联真实状态同步：
  - `viewed` 同步目标：品牌经理 / 新媒体经理-品牌经理 / 活动策划执行
  - `interested` 同步目标：品牌平面设计师（天津小谷顿餐饮管理服务有限公司）
  - `notfit` 同步目标：10 条不合适名单（高级平面设计师、视觉设计师、品牌设计/平面视觉设计、品牌设计师双休14薪、品牌设计师、营销视觉设计师-社媒方向、品牌设计师、视觉设计师、营销策划经理、摄影俱乐部活动策划）
- 初次同步失败的原因是：智联库里很多 job 记录缺失公司名，导致无法按 `岗位+公司` 精准匹配应用记录。
- 随后补做“智联公司名回填”：从 report 原始 `raw_result.*.title` 中解析公司名，回写到 `jobs.company`，虽然最初正则有脏数据（公司名前多出“年”），但后续已统一清洗掉该前缀，修正了 12 条公司记录，例如：
  - 深圳华强
  - 深圳市恒冠投资发展有限公司
  - 深圳市乐有家控股集团有限公司
  - 科锐尔人力资源服务(苏州)有限公司
  - 深德科
  - 天津小谷顿餐饮管理服务有限公司
  - 深圳协同创新高科技发展有限公司
  - 深圳市华风正茂汽车有限公司
  - 昊宇视觉科技(深圳)有限公司
  - 深圳市悦美创新科技发展有限公司
- 在完成公司名回填后，再次做智联真实状态同步，结果：
  - viewed=3
  - interested=1
  - notfit=10
  已可通过 `applications.notes` / `status` / `platform_status` 把平台前台真实进展落库（至少针对今天这批已对齐名单）。

### 前程无忧页面真实状态同步（下午后段）
- 针对前程无忧今日 4 条真实申请，执行了数据库映射修复：
  - 品牌设计师｜深圳立墨文化传播有限公司 → `applied + submitted`
  - 品牌设计师｜优之科技（深圳）有限公司 → `applied + submitted`
  - 品牌策划专员｜深圳市欣博跃电子有限公司 → `applied + already_applied`
  - 品牌营销负责人/双休/办公地罗湖｜深圳市武测空间信息有限公司 → 初次未对上，后经单独查 jobs 表定位为 `job_id=803`，已补插入 `applications`（`app_id=29`，`platform_status=already_applied`）
- 截至当前，前程无忧今日页面真实 4 条已全部能在 `applications` 中找到对应记录。

### 用户偏好 / 交付约束（今日下午新增）
- 用户再次强调：
  - “我必须要每一条完整的字段都能展现出来”
  - 求职结果表格的 14 个字段必须全部补齐，**不允许出现“未提取 / 待补充”**。
- 这被提升为硬规则：
  - 以后求职结果汇报必须先补字段，再出“精致表格”
  - 不完整 = 不交付
- 用户随后明确表示：本批结果先不纠结精致表格，优先修好**投递记录精准倒车入库**。因此本轮工作主线已正式转为“平台真实状态 ↔ 数据库应用记录”的一致性建设。

### 当前阶段结论（截至 2026-03-18 15:17 左右）
- 平台真实口径已掌握：
  - 智联总投递 31，前程无忧总投递 18
  - 今日累计投递：智联 14，前程无忧 4
- 数据库主链已从“部分入库 + 重复插入 + 状态不统一”修到：
  - 主业务库已确认
  - 对账补写已执行
  - 重复记录已压平
  - 智联/前程无忧关键 `platform_status` 已统一到 `submitted / already_applied`
  - 正式代码主链已加入幂等 upsert 能力
- 仍待后续继续正式化的事项：
  1. 将今天这些“平台真实进展同步回 DB”的临时脚本继续收进正式代码主链（不是长期依赖临时脚本）
  2. 构建“平台真实页面 → applications 状态回写”的标准流程
  3. 将“字段必须补齐后才允许输出精致表格”的规则接入正式结果输出链路
